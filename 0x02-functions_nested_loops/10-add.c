#include"main.h"

/**
 * * int add(int, int) - that adds two integers and returns the result
 * * @a: is the input of the function
 * * @b: is the input of the function
 * * Return: return adds of two integers
*/

int add(int a, int b)
{
	int add;

	add = a + b;
	return (add);

}
